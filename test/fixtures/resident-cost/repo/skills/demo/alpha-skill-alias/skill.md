---
name: alpha-skill
description: Alias directory reusing the alpha-skill name, for duplicate_names.
disable-model-invocation: true
coordination:
  internal: false
---

# Alpha Skill Alias

Fixture only. Deliberately duplicates the `alpha-skill` frontmatter name from
a different directory, to exercise `duplicate_names`.
